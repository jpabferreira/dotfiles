# Zsh config Fixes
# Change the color of comments to prevent them from becoming invisible (same fg
# color as the bg) using the railscasts theme.
# ZSH_HIGHLIGHT_STYLES[comment]='fg=8'
#
###### Powerlevel10k Overrides ######
# Moved to .p10k.zsh
# # Add an empty line before each prompt.
# typeset -g POWERLEVEL9K_PROMPT_ADD_NEWLINE=true
# # OS identifier color.
# typeset -g POWERLEVEL9K_OS_ICON_FOREGROUND=0
# # Current directory background color.
# typeset -g POWERLEVEL9K_DIR_BACKGROUND=1
# # Default current directory foreground color.
# typeset -g POWERLEVEL9K_DIR_FOREGROUND=7
# # Green prompt symbol if the last command succeeded.
# typeset -g POWERLEVEL9K_PROMPT_CHAR_OK_{VIINS,VICMD,VIVIS,VIOWR}_FOREGROUND=2
# # Red prompt symbol if the last command failed.
# typeset -g POWERLEVEL9K_PROMPT_CHAR_ERROR_{VIINS,VICMD,VIVIS,VIOWR}_FOREGROUND=1
# # Default prompt symbol.
# typeset -g POWERLEVEL9K_PROMPT_CHAR_{OK,ERROR}_VIINS_CONTENT_EXPANSION='󰛂 '
# # Prompt symbol in command vi mode.
# typeset -g POWERLEVEL9K_PROMPT_CHAR_{OK,ERROR}_VICMD_CONTENT_EXPANSION='󰛁 '
# # Show exit status icons for the last command
# typeset -g POWERLEVEL9K_STATUS_OK=true
# typeset -g POWERLEVEL9K_STATUS_ERROR=true
# # Background jobs color.
# typeset -g POWERLEVEL9K_BACKGROUND_JOBS_FOREGROUND=0
# typeset -g POWERLEVEL9K_BACKGROUND_JOBS_BACKGROUND=14
# # Don't show the number of background jobs.
# # Custom icon.
# typeset -g POWERLEVEL9K_BACKGROUND_JOBS_VISUAL_IDENTIFIER_EXPANSION=' '
# typeset -g POWERLEVEL9K_BACKGROUND_JOBS_VERBOSE=true
#
# typeset -g POWERLEVEL9K_STATUS_OK_VISUAL_IDENTIFIER_EXPANSION=' '
# typeset -g POWERLEVEL9K_STATUS_OK_PIPE_VISUAL_IDENTIFIER_EXPANSION=' '
# typeset -g POWERLEVEL9K_STATUS_ERROR_VISUAL_IDENTIFIER_EXPANSION=' '
# typeset -g POWERLEVEL9K_STATUS_ERROR_SIGNAL_VISUAL_IDENTIFIER_EXPANSION=' '
# typeset -g POWERLEVEL9K_STATUS_ERROR_PIPE_VISUAL_IDENTIFIER_EXPANSION=' '
#
#
